import { makeStyles } from "@material-ui/core";

export const cinemaStyles = makeStyles(() => ({
    customColorTab: {
        '.MuiTabs-indicator': {
            background: '#ff5722'
          }
    }
  
}))