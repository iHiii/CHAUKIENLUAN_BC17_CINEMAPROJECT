import { Typography } from "@mui/material";

const TitleTicketRoom: React.FC<any> = (props) => {
  return (
    <span style={{ color: "orangered", fontSize: 30, fontFamily: "Roboto", fontWeight:'bold'}}>
      CHI TIẾT PHÒNG VÉ
    </span>
  );
};

export default TitleTicketRoom;
